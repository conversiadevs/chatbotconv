from setuptools import setup, find_packages

setup(
    name="rag",  
    version="0.1.0",  
    author="Conversia",
    author_email="conversia.devs@gmail.com",
    packages=find_packages(),  # Automatically finds all packages in the directory
)